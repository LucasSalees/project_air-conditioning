spring.application.name=project_air-conditioning

server.port=${PORT:10000}

# URL configurada para conexão direta (Porta 5432)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USER:postgres.hyixjbewdcmovnsirkdl}
spring.datasource.password=${DB_PASSWORD:SenhaWin2026!}
spring.datasource.driver-class-name=org.postgresql.Driver

# Se você mudar o código e precisar criar tabelas, use 'update' localmente e depois volte para 'none'.
spring.jpa.hibernate.ddl-auto=none

# Desativamos logs pesados no console durante o início
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Removido: spring.jpa.database-platform (O Hibernate 6 detecta automaticamente)
spring.jpa.open-in-view=false

api.security.token.secret=${JWT_SECRET:ChaveDeTesteParaDesenvolvimentoLocal123}

# --- CONFIGURAÇÃO HIKARI (MANUTENÇÃO DE CONEXÃO) ---
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.max-lifetime=45000
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5